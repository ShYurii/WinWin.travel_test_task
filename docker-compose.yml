services:

  postgres:
    image: postgres:18
    container_name: winwin-postgres
    environment:
      POSTGRES_DB: winwin_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"