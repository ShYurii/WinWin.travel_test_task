spring.application.name=auth-api
server.port=8080
#spring.datasource.url=jdbc:postgresql://localhost:5432/winwin_test
spring.datasource.url=${POSTGRES_URL:jdbc:postgresql://localhost:5432/winwin_test}

#for local
#spring.datasource.username=postgres
#spring.datasource.password=root

# for Docker
spring.datasource.username=${POSTGRES_USER:postgres}
spring.datasource.password=${POSTGRES_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

jwt.secret=${JWT_SECRET}
internal.token=${INTERNAL_TOKEN}
data.api.url=${DATA_API_URL:http://localhost:8081}


